paths:
  static: "public"
  uploads: "public/uploads"

collections_config:
  artiklar:
    path: data/blog
    url: "/blog/[slug]"
    output: true
    _enabled_editors: ["content", "data"]
    _inputs:
      date:
        type: date
