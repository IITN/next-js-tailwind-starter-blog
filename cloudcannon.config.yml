paths:
  static: "public"
  uploads: "public/uploads"

collections_config:
  articles:
    name: Artiklar
    path: data/blog
    url: "/blog/[slug]"
    output: true
    _enabled_editors: ["content", "data", "visual"]
    writers:
      type: multiselect
      options:
        values:
          - Bolt
          - Nut
          - Gear
    _inputs:
      date:
        type: date
      writers:
        type: multiselect
        options:
          values: authors

  authors:
    name: Författare
    path: data/authors
    output: true
    _enabled_editors: ["content", "data"]
