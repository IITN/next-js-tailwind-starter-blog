paths:
  static: "public"
  uploads: "public/uploads"

collections_config:
  articles:
    name: Artiklar
    path: data/blog
    url: "/blog/[slug]"
    output: true
    _enabled_editors: ["content", "data", "visual"]
    spare_part: Bolt
    _inputs:
      spare_part:
        type: select
        options:
          values:
            - Bolt
            - Nut
            - Gear

  authors:
    name: Författare
    path: data/authors
    output: true
    _enabled_editors: ["content", "data"]
