paths:
  static: "public"
  uploads: "public/uploads"

# file structure
_structures:
  _inputs:
    image:
      type: text
  document:
    values:
      - value:
          name:
          description:
          image:

collections_config:
  articles:
    name: Artiklar
    path: data/blog
    url: "/blog/[slug]"
    output: true
    _enabled_editors: ["content", "data", "visual"]
    _inputs:
      authors:
        type: multiselect
        options:
          values: collections.authors
      date:
        type: date
      documents:
        type: array
        options:
          structures: _structures.document

  authors:
    name: Författare
    path: data/authors
    output: true
    _enabled_editors: ["content", "data"]
